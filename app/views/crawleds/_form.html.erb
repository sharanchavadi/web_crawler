<%= form_with(model: crawled, local: true) do |form| %>
  <% if crawled.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crawled.errors.count, "error") %> prohibited this crawled from being saved:</h2>

      <ul>
      <% crawled.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
